安装后可通过 `python -m hifi_detector.cli analyze <file>` 或 `python -m hifi_detector.cli batch <dir> [--recursive] [--json] [--output report.json]` 运行；Web 模式 `python -m hifi_detector.cli web --host 0.0.0.0 --port 8099` 会启动 FastAPI 并在默认浏览器打开 `http://host:port`。